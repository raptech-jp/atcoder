#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main(){
    int n,q,dist;
    string hand;
    int left=0, right=1;

    cin >> n >> q;
    vector<int> a(n);

    for (int i = 0; i < q; i++)
    {
        cin >> hand >> dist;
    }
    

    return 0;
}
